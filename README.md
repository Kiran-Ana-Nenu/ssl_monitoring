
# SSL Monitor - Enterprise Grade SSL Certificate Monitoring (Generated)

This repository is an autogenerated starter for an enterprise SSL certificate monitoring system using:
- Django (default auth)
- Celery + Redis (async checks)
- PostgreSQL (recommended)
- HTMX + Tailwind CSS (simple UI)
- Docker Compose for local/dev deployment

## Structure
- monitoring: models, tasks
- dashboard: views, templates, HTMX fragments
- notifications: simple notification service
- ssl_monitor: Django settings & Celery wiring

## Quick start (development)
1. Copy `.env.template` to `.env` and edit values.
2. Build & run with Docker Compose:
   ```
   docker compose up --build
   ```
3. Create superuser:
   ```
   docker compose exec web python manage.py createsuperuser
   ```

